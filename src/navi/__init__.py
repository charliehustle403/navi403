"""Navi — read-only Technical Workbench AI backend.

Milestone 1 ships the skeleton + data layer only: config, the SQLModel schema (build
spec §5), the Alembic migration, and a DB-checking ``/health`` endpoint. The tool broker,
tools, model client, and Navi loop arrive in later milestones.
"""

__version__ = "0.1.0"
